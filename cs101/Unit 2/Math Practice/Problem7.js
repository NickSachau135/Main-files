let intValue = Number(prompt("What is the initial value?"));
let difference = Number(prompt("What is the difference?"));
let n = Number(prompt("What is the value of n?"));

let value = intValue + (n -1)*difference;
console.log(`The value is ${value}.`);